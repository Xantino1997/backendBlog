name: my-app

services:
  web:
    build: .
    env:
      PORT: $${{PORT}}
    command: yarn start
    routes:
      - path: /
        port: web
  mongodb:
    image: mongo:4
    env:
    REACT_APP_URI: my-app
